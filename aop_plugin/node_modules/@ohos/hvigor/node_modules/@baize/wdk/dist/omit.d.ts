/**
 * 创建一个从 object 中忽略的属性的对象，返回忽略属性后的新对象
 * 反向版pick。这个对象由忽略属性之外的object自身和继承的可枚举属性组成。（注：可以理解为删除object对象的属性）。
 * @param input 来源对象
 * @param ignoreProps 保留的属性
 */
export default function omit(input: any, ...ignoreProps: any[]): object;
