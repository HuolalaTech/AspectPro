/**
 * 将数组array的元素通过iteratee进行值映射，计算映射后的值的平均值
 * @param array，待处理的数组
 * @param iteratee，用于对数组元素的值进行映射（转换）
 * @return 一个平均值，或者NaN（如果计算结果非number）
 */
declare function meanBy(array?: any[], iteratee?: Function | string): number;
export default meanBy;
