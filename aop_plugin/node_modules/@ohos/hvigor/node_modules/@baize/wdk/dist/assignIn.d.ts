/**
 * 分配来源对象（sources）的可枚举属性及其继承的属性到目标（target）。
 * 来源对象的应用规则是从左到右，随后的下一个对象的属性会覆盖上一个对象的属性
 * @param target
 * @param sources
 */
export default function assignIn(target: object, ...sources: any[]): object;
