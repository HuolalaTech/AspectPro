/**
 * 创建一个按顺序排列的唯一值的数组。所有给定数组的元素值使用SameValueZero做等值比较。（注： arrays（数组）的并集，按顺序返回，返回数组的元素是唯一的）
 *
 * @since 1.0.0
 * @returns (Array): 返回一个新的联合数组。
 *
 * @example
 * ```
 * _.union([2], [1, 2]);
 * // => [2, 1]
 * ```
 */
export default function union(...arrays: any[]): any[];
