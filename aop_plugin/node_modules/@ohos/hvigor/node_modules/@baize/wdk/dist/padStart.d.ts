/**
 * 如果string字符串长度小于 length 则在左侧填充字符。 如果超出length长度则截断超出的部分。
 * @param input   要填充的字符串
 * @param length  填充的长度
 * @param chars   填充的字符
 */
export default function padStart(input: any, length?: number, chars?: string): string;
