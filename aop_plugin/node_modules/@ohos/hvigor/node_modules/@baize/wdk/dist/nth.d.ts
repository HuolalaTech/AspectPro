/**
 * 获取`array`数组的第n个元素。如果n为负数，则返回从数组结尾开始的第n个元素。
 * @since 1.0.0
 * @param array (Array): 要查询的数组。
 * @param [n=0]  (number): 要返回元素的索引值。
 * @returns (*): 获取array数组的第n个元素。
 * @example
 * var array = ['a', 'b', 'c', 'd'];
 *
 * _.nth(array, 1);
 * // => 'b'
 *
 * _.nth(array, -2);
 * // => 'c';
 */
export default function nth(array: any[], n?: any): any;
