import { PredicateParam, PredicateParamFunc } from './common/baseCompare';
/**
 * 在数组中，自右向左查找出满足条件的第一个元素的索引
 * 返回找到元素的 索引值（index），否则返回 -1
 * @param array
 * @param predicate
 * @param start
 */
export default function findLastIndex(array: any, predicate?: PredicateParam | PredicateParamFunc, start?: any): number;
