declare type ObjIterator<Obj extends object> = (val: Obj[keyof Obj], key: keyof Obj) => any;
/**
 * 遍历对象自己的可枚举的属性，调用传入的迭代函数，迭代函数返回false时，提前退出遍历
 * @param obj
 * @param iteratee
 */
export default function forOwn<T extends object>(obj: T, iteratee: ObjIterator<T>): T;
export {};
