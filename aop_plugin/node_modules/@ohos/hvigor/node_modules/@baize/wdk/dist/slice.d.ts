/**
 * 裁剪数组/类数组array，从 start 位置开始到end结束，但不包括 end 本身的位置
 * 返回 数组array 裁剪部分的新数组
 * @param input 要裁剪数组
 * @param start 开始位置
 * @param end   结束位置
 */
export default function slice(input: any, start?: any, end?: any): any[];
