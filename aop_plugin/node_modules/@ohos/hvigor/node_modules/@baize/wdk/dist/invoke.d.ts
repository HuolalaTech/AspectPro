/**
 * 调用obj中path所指向的函数并返回结果
 * @param obj，要调用的函数所属的对象
 * @param rawPath，obj的属性路径，用于指定要调用的函数
 * @param args，调用函数时传入的参数
 * @return 返回调用函数得到的结果
 */
declare function invoke(obj?: Object, rawPath?: string | string[], ...args: any[]): any;
export default invoke;
