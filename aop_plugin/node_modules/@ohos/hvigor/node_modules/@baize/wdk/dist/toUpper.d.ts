/**
 * 转换整个string字符串的字符为大写
 * @param str (string) 待转换的字符串
 * @example
 * _.toUpper('--foo-bar--');
 * // => '--FOO-BAR--'
 *
 * _.toUpper('fooBar');
 * // => 'FOOBAR'
 *
 * _.toUpper('__foo_bar__');
 * // => '__FOO_BAR__'
 */
export default function toUpper(input: string): string;
