/**
 * 判断一个key是否为对象的直接属性
 *
 * @param {Object} object 要查询的对象
 * @param {string | Array} path 要判断的key
 * @returns {boolean} 如果该key为该对象的直接属性，返回true，否则false
 */
export default function has(object: Object, path: string | any | any[]): boolean;
