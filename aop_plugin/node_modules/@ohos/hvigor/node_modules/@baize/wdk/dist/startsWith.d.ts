/**
 * 检查字符串string是否以 target 开头
 * @param str: 要检索的字符串
 * @param target: 要检查的字符串
 * @param position: 检索的位置
 */
export default function startsWith(str: string, target: string, position?: any): boolean;
